# 参与贡献

欢迎通过 Issue 或 Pull Request 改进电商服装换装导演。

## 适合提交的内容

- 修正文档、示例或安装步骤。
- 补充可复用的电商换装参数方案。
- 改善商品与搭配映射、首帧提示词、视频时间轴和商业质检清单。
- 补充稳定、可复现且不误导买家的换装机制。
- 修复不同章节之间互相冲突的默认值或约束。

## 修改原则

- 当前商业版 Skill 名称为 `ecommerce-fashion-tryon-director`，除非是经过讨论的破坏性升级，不要随意更改。
- 用户明确参数必须高于默认参数。
- 参考模特模式下，模特参考图是必填项；没有图时应要求补充或改用原创模特。
- 服装图信息高于文本补全。看不清的商品细节应标记为 `需人工确认`。
- 保持商品核心版型、颜色、图案、领口、袖型、下摆、口袋、扣件、拉链和 logo 位置。
- 不要凭空添加品牌、联名、授权、功能面料、平台标识、价格或促销承诺。
- 所有模特内容都应明确为成年人。
- 修改视频机制时，必须保持商品可见、SKU 因果清楚、换装后只保留一个主模特。
- 不要把仓库文档复制进 `skill/`；该目录只保留运行时所需文件。

## 提交前检查

1. 确认 `skill/SKILL.md` 的 YAML frontmatter 有效。
2. 确认 `skill/SKILL.md` 中的默认参数与 README、首次使用教程一致。
3. 确认 `references/parameter-presets.md` 中的方案能映射到真实电商用途。
4. 确认 `references/transition-library.md` 的机制没有破坏商品保真和模特一致性。
5. 检查中文标点、画幅、时长、套数和输出模式是否自洽。
6. 不要提交参考模特照片、服装商品图、生成结果、压缩包、密钥或个人信息。

## Issue 建议格式

- 当前行为
- 期望行为
- 使用的输入参数
- 是否有模特图或服装图
- 涉及的输出模式、画幅、时长和换装机制
- 可复现步骤
